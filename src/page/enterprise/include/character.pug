
section.character.container
  h2.section-title 团队介绍
  .character-wrapper.row
    each item in characterList
      a.col-12.col-md-6.col-sm-6.col-lg-3
        .character-item
          .character-image
            img(src=item.img, alt=item.name)
          .character-info
            span.name #{item.name}
            span.cv #{item.cv}